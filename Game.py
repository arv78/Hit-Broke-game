# input (file or user_input or random)
# choose the starter of the game

#assign the inputs to the global Arrays
#define func (distance):
    ''' for Arr1 (i)
           for Arr2 (j)
               if Arr1[i] + Arr2[j] < distance then
               ** we need a counter to determine how many lvl's we should continue **
                  func (distance -(Arr1[i] + Arr2[j]))  *(still in progress)*
               elif Arr1[i] > distance then
                  return true  *(we won)*
               else
                  break
'''
    
